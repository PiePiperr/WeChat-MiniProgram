<button class='save' bindtap='save'>保存</button>
<view class='tip'>*以下企业家均来自“IT桔子创投人物” </view>
<view class='page'>

  <view class='ch_entre'>
    <view class='text'>已选</view>
    <view wx:if='{{myentre==""}}' class='none'>无</view>
    <block wx:for="{{myentre}}" wx:key="index">
      <view class='my_entre'>{{item}}
        <image src='../../icon/delete.png' class='delete' mode='aspectFit' bindtap='delete' data-Index="{{index}}"></image>
      </view>
    </block>
  </view>


  <view class="searchArea">
    <view class="searchInput">
      <image src="/icon/search.png"></image>
      <input confirm-type="search" bindconfirm="Search_Entre" value='{{inputText}}' style='font-size:12pt; display:inline-block;' bindinput="bindKeyInput" bindfocus='bindfocus'></input>
    </view>
    <view bindtap="Search_Entre" hidden='{{!search}}' class='clearSearch'>搜索</view>
    <view bindtap="clcSearch_entre" hidden='{{search}}' class='clearSearch'>取消</view>
  </view>
  <view hidden="{{loadingIconHidden}}" class="loading">
    <view class="loadingText">
      <text>正在加载\n</text>
    </view>
    <image src="/icon/loading.gif" mode='aspectFill'></image>
  </view>

  <view class="EntreList" hidden='{{!entre_List}}'>
    <block wx:for="{{EntreList}}" wx:key="index" wx:for-item="EntreList">
      <view bindtap="choose" data-id='{{EntreList}}' class='entre'>
        {{EntreList}}
      </view>
    </block>
  </view>

  <view class="EntreList" hidden='{{!searResult}}'>\
    <view bindtap="choose" class='entre' data-id='{{searchResult}}'>{{searchResult}}</view>
  </view>

  <view hidden="{{noResultHidden}}" class="noResult">暂无搜索结果</view>
</view>